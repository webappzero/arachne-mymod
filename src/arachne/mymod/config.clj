(ns arachne.tmpl.config
  "The simplest possible config"
  (:require [arachne.core.config :as cfg]))


(defn tmpl-config
  "Consume and return the same config. This useless function may be deleted."
  [cfg]
  cfg)


